FILE (GLOB SOURCES RELATIVE "${CMAKE_CURRENT_SOURCE_DIR}" *.cpp)

INCLUDE_DIRECTORIES (${FOX_INCLUDE_DIRS})
INCLUDE_DIRECTORIES (${X11_X11_INCLUDE_PATH})
INCLUDE_DIRECTORIES (${CMAKE_SOURCE_DIR}/jef)

ADD_LIBRARY (
    mnumgr
    STATIC
    ${SOURCES}
)

