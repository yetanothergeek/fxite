--- ../fox-1.7.29.orig/lib/FXFileList.cpp	2011-08-09 22:30:56.000000000 -0500
+++ ./lib/FXFileList.cpp	2011-08-09 22:38:00.000000000 -0500
@@ -1492,7 +1492,11 @@
 
   // Are we at the top directory?
   istop=FXPath::isTopDirectory(directory);
-
+#ifdef WIN32
+  if ((directory.length()==3) && (directory[1]==':') && (directory[2]=='\\')) {
+     directory.trunc(2);
+  }
+#endif
   // Get directory stream pointer
   if(dir.open(directory)){
 
