--- ../fox-1.7.31.orig/lib/FXDirList.cpp	2011-10-06 10:25:40.000000000 -0500
+++ ./lib/FXDirList.cpp	2011-10-20 07:47:38.000000000 -0500
@@ -916,6 +916,11 @@
   // Path to parent node
   directory=getItemPathname(par);
 
+#ifdef WIN32
+  if ((directory.length()==3) && (directory[1]==':') && (directory[2]=='\\')) {
+    directory.trunc(2);
+    }
+#endif
   // Managed to open directory
   if(dir.open(directory)){
 
